{
  "name": "pozdno",
  "version": "1.0.0",
  "description": "This is platform for study ",
  "main": "main.js ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run serve --prefix client\" \"nodemon main.js\"",
    "build": "npm run serve --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "vue-router": "^3.4.3"
  },
  "devDependencies": {
    "@vue/cli-plugin-router": "^4.5.4"
  }
}
